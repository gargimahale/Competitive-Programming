
#include <iostream>


int x{ 0 };

namespace one {
	int x{ 1 };
	int y{ 10 };
	namespace one_one {
		int x = 11;
		int z = 25;
	}
}

namespace two{
	int x{ 2 };
}



int main() {


	//local >> global == explicitly declared namespacee
	//int x = 100;//local variable is always the one to refer to
	//using namespace one;
	using namespace one::one_one;
	using namespace two;
	std::cout << one::one_one::x<< " " << ::x << " " <<one::x << " " << two::x << " " <<std::endl; //global variable

	//std::cout << x << std::endl; //global variable

	std::cout << z << std::endl;

	//namespace alias
	namespace O2 = one::one_one;
	using namespace O2;
	std::cout << O2::x << std::endl;

	return 0;
}