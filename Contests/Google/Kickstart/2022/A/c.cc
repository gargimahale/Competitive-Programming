#include <bits/stdc++.h>
using namespace std;

int main(){
	int T;
	for (int i = 0; i < T; ++i){
		long long length;
		string input;
		string result = "IMPOSSIBLE";

		cin >> length >> input;

		

		cout << "Case #" << i+1 << ": " << result << "\n";
	}
}