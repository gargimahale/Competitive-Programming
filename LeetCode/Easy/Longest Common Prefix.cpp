#include <bits/stdc++.h>

using namespace std;

class Solution {
public:
    string longestCommonPrefix(vector<string>& s) {
        // vertical scaling
        if (s.empty()) return "";
        for (int j = 0; ; ++j) {
            for (int i = 0; i < s.size(); ++i) {

            }
        }
    }
};